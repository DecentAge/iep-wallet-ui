{
  "name": "iep-wallet-ui-e2e",
  "private": true,
  "description": "Playwright end-to-end tests for iep-wallet-ui. Isolated from the root project to avoid the Angular 6 / rxjs-compat / node-sass peer-dep noise.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "snapshot": "playwright test --update-snapshots",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.4.0"
  }
}
